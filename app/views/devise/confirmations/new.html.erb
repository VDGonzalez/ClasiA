<div class="container margin-container bg-white">

	<div class="row header-form">
		<div class="col-xs-push-1 col-xs-10 text-center">
			<h2 class="h-padding">Reenviar instrucciones de confirmaci&oacute;n</h2>
		</div>
	</div>
	<div class="row">
		<%= simple_form_for(resource, as: resource_name, url: confirmation_path(resource_name), html: { method: :post }) do |f| %>
		  

		<div class="col-xs-offset-1 col-xs-10 col-md-offset-3 col-md-6">
			<!-- ALERTA -->
			<%= f.error_notification %>
		  	<%= f.full_error :confirmation_token %>
			<!-- INPUTS -->
			  	<div class="form-group">
			  		<label for="email">Email:</label>
			  		<%= f.input :email, required: false, autofocus: true, label: false, input_html: { class: 'form-control ' } %>
				</div>

			<!-- BOTONES -->
			<div class="row">
				<div class="col-xs-6">
					<div class="form-group">
						    <%= f.button :submit, "Reenviar instrucciones de confirmación", class: 'btn btn-success' %>
					</div>
				</div>			  		
				<div class="col-xs-6">
					<div class="form-group text-right">
						<%= render "devise/shared/links" %> 
					</div>	 
				</div>	
			</div>
		</div>
		<% end %>	      
  	</div>	
</div>